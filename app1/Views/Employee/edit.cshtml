@model EmpPlusDeptList

<form asp-action="saveedit" asp-controller="Employee" asp-route-id="@Model.ID" method="post">
	
	 <label>Name</label>
   	<input type="text" value="@Model.Name" class="form form-control" name="Name" id="Name" />
  	@* @Html.TextBox("Name") *@
@*   @Html.LabelFor(x=>x.Name)
  @Html.EditorFor(x=>x.Name) *@
@*  @Html.TextBoxFor(x=>x.Name,new{Style="color:red",@class="form form-control"})
 *@ <label>Address</label>
	<input type="text" value="@Model.Address" class="form form-control" name="Address" id="Address" />
	<label asp-for="JobTitle"></label>
	<input asp-for="JobTitle" class="form form-control" />
	

<label asp-for="Salary"></label>
	<input  asp-for="Salary" class="form form-control" />


	 	<label asp-for="imgURL"></label>
	<input asp-for=imgURL class="form form-control" />
	<label>Dept Name</label>
@* 	 <input type="number" value="@Model.DeptId" class="form form-control" name="DeptId" id="DeptId" />
 *@

	   <select name="DeptId"  id="DeptId" class="form form-control" onselect="@Model.DeptId" >
	    @foreach (var item in Model.DepartmentList)
	    	{
				if(Model.DeptId== item.Id)
			{
	    		<option value="@item.Id"  selected>
	    			@item.Name
				</option>
			}
			else
			{
				<option value="@item.Id">
					@item.Name
				</option>

				
			}
		}

	   </select>  
	@*  <select asp-for="DeptId" asp-items="deps" class="form-control">
		 	<option></option>
		 </select>
 *@
	<input type="submit" value="Save" class="btn btn-success" name="submit" id="submit" />



</form>

<a asp-action="index" asp-controller="Employee">Back</a>